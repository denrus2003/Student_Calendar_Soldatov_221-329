alembic==1.12.0           # Проверено, совместимо с Python 3.8
blinker==1.8.2            # Проверено, совместимо с Python 3.8
cachetools==5.3.1         # Проверено, совместимо с Python 3.8
certifi==2023.7.22        # Проверено, совместимо с Python 3.8
charset-normalizer==3.3.0 # Проверено, совместимо с Python 3.8
click==8.1.7              # Проверено, совместимо с Python 3.8
colorama==0.4.6           # Проверено, совместимо с Python 3.8
Flask==2.3.3              # Совместимо с Python 3.8
Flask-Login==0.6.3        # Проверено, совместимо с Python 3.8
Flask-Mail==0.9.1         # Проверено, совместимо с Python 3.8
Flask-Migrate==3.1.0      # Проверено, совместимо с Python 3.8
Flask-SQLAlchemy==2.5.1   # Проверено, совместимо с Python 3.8
Flask-WTF==0.15.1         # Проверено, совместимо с Python 3.8
google-api-core==2.17.0   # Проверено, совместимо с Python 3.8
google-api-python-client==2.95.0  # Проверено, совместимо с Python 3.8
google-auth==2.35.0       # Проверено, совместимо с Python 3.8
google-auth-httplib2==0.2.0  # Проверено, совместимо с Python 3.8
google-auth-oauthlib==1.1.0 # Проверено, совместимо с Python 3.8
googleapis-common-protos==1.60.0 # Проверено, совместимо с Python 3.8
httplib2==0.22.0          # Проверено, совместимо с Python 3.8
idna==3.10                # Проверено, совместимо с Python 3.8
itsdangerous>=2.1.2       # Совместимо с Flask 2.3.3 и Python 3.8
Jinja2==3.1.2             # Совместимо с Flask 2.3.3 и Python 3.8
Mako==1.2.2               # Проверено, совместимо с Python 3.8
MarkupSafe>=2.1.1         # Обновлено, совместимо с Flask 2.3.3 и другими пакетами
oauthlib==3.1.1           # Проверено, совместимо с Python 3.8
proto-plus==1.22.2        # Проверено, совместимо с Python 3.8
protobuf==3.20.3          # Проверено, совместимо с Python 3.8
pyasn1==0.5.0             # Проверено, совместимо с Python 3.8
pyasn1_modules==0.3.0     # Проверено, совместимо с Python 3.8
PyMySQL==1.1.1            # Проверено, совместимо с Python 3.8
pyparsing==3.1.0          # Проверено, совместимо с Python 3.8
requests==2.31.0          # Проверено, совместимо с Python 3.8
requests-oauthlib==1.3.0  # Проверено, совместимо с Python 3.8
rsa==4.8                  # Проверено, совместимо с Python 3.8
SQLAlchemy==1.4.47        # Проверено, совместимо с Python 3.8
typing_extensions==4.0.1  # Проверено, совместимо с Python 3.8
uritemplate==3.0.1        # Проверено, совместимо с Python 3.8
urllib3==1.26.16          # Проверено, совместимо с Python 3.8
Werkzeug==2.3.7           # Совместимо с Flask 2.3.3 и Python 3.8
WTForms==3.0.0            # Проверено, совместимо с Python 3.8